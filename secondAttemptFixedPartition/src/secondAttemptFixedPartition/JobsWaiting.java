package secondAttemptFixedPartition;
import java.util.*;
public class JobsWaiting 
{
/*	protected ArrayList<Job> jobsWaiting;
	public JobsWaiting()
	{
		super(size, name);
	}
	public void printJobsWaiting()
	{
		for(int i = 0; i < jobsWaiting.size(); i++)
		{
			System.out.println((i+1)+ ") "+jobsWaiting.get(i).getName());
		}
	}
	public void clearJob(int i)
	{
		Scanner in = new Scanner(System.in);
		String ans;
		//int num;
		//printJobsWaiting();
		//System.out.println("What job would you like to remove?");
		//num = (in.nextInt()-1);
		System.out.println("The job you want to remove is : " +jobsWaiting.get(i).getName()+"\n Are you sure you want to remove it?");
		ans = in.nextLine().toLowerCase();
		if(ans.equals("yes"))
		{
			jobsWaiting.remove(i);
			System.out.println("The job was removed.");
		}
		else
		{
			System.out.println("The job was not removed.");
		}
	}
	public void updateJobsWaiting(int k, Job[] j)
	{
		for(int w = 0; w < j.length; w++)
		{
			if(j[w].getTime() != 0)
			{
				System.out.println(j[w].getName()+" is complete.");
				if(j[w+1] != null)
				{
					j[w]=j[w+1];
				}
				else
				{
					j[w] = null;
				}
				
			}
		}
	}*/
		}

